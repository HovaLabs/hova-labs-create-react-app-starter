import { breakpoints } from './breakpoints';
import { uiLight } from './colors';
import { elevation } from './elevation';
import { radii } from './radii';
import { spacing } from './spacing';
import { typography } from './typography';

export const theme = {
  breakpoints,
  colors: uiLight,
  elevation,
  radii,
  spacing,
  typography,
};
